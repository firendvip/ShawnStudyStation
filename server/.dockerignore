# 依赖在镜像内重新安装,不拷贝宿主机的 node_modules
node_modules

# 绝不把本地密钥带进镜像;生产环境变量由 compose 注入
.env
.env.*
!.env.example

# 本地 SQLite / 数据文件(后端用 PostgreSQL,这些是历史/本地产物)
data/*.db
data/*.db-journal
data/*.sqlite

# 日志与杂项
*.log
npm-debug.log*
.DS_Store

# 版本控制与文档
.git
.gitignore
README.md
Dockerfile
.dockerignore
