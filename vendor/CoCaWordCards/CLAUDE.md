# 项目强制规则

## 沟通：极致简洁（强制）

每次对话完成后，用最简洁的语言汇报结果——只给结论与必要的下一步，省略过程与铺陈。
需要用户协助/决策时，同样用最简语言说清要他做什么即可。此规则适用于本项目所有对话。

## 主动执行（强制）

不要问「还要调整别的吗 / 要不要…」这类征询性问题。当有可改进项时，直接按你更推荐的方式做出来，
完成后在用户浏览器中打开（webapp 出新版本时主动 `open` 对应地址）。只在确实无法自行决策时才发问。
