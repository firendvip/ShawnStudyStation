# 依赖在镜像内重新安装
node_modules

# 构建产物在镜像内重新生成
.next
out
build

# 环境密钥绝不进镜像(生产由 compose 注入),但保留示例
.env
.env.*
!.env.example

# 本地 SQLite 数据库:运行时使用挂载卷上的 /data/pinpin.db,不拷贝本地副本
*.db
*.db-journal
prisma/*.db
prisma/*.db-journal

# 本地生成的报告
storage

# 杂项 / 版本控制
.git
.DS_Store
*.log
coverage
.vercel
*.tsbuildinfo
Dockerfile
.dockerignore
